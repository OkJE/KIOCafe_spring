<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cart">

<insert id="insertCart" parameterType="cart">
      insert into kiocafe_cart values
      (#{dnum},#{dname},#{dprice},#{dqty},#{dtotal},#{dpicture}, #{did})        
   </insert>
   

	<select id="cartList" parameterType="map" resultType="cart">
		select * from kiocafe_cart
	</select>
	
	<delete id="cartDelete" parameterType="hashmap">
		DELETE FROM kiocafe_cart
		where did = #{userid}
		and dnum IN
		<foreach collection="didlist" item="item" index="index"
			separator="," open="(" close=")">
			#{item}
		</foreach>
	</delete>

<!--    <select id="categoryCount" parameterType="String" resultType="int">
      select nvl(count(*),0) from kiocafe_category where cid = #{value}
   </select>

   <select id="categoryList" parameterType="map" resultType="category">
      select * from
      (select rownum rnum, a.* from (select * from kiocafe_category where cid = #{cid}) a )
      where rnum between #{start} and #{end}      
   </select>
   
   
   <insert id="insertCategory" parameterType="category">
      insert into kiocafe_category values
      (#{cnum},#{cname},#{cmenu},#{ctext},#{cprice},0,#{cpicture}, #{cid})        
   </insert>
 
   
   <select id="categoryOne" parameterType="int" resultType="category">
      select * from kiocafe_category where cnum=#{cnum}
   </select>

   
   
   <update id="categoryUpdate" parameterType="category">
      update kiocafe_category set cname = #{cname} , ctext = #{ctext} , 
      cpicture = #{cpicture} where cnum = #{cnum}      
   </update>


   <delete id="categoryDelete" parameterType="int">
   delete from kiocafe_category where cnum= #{cnum}   
   </delete>  -->

</mapper>